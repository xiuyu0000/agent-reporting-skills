interface:
  display_name: "Single-Reviewer Approval Delivery"
  short_description: "Route qualifying plans to two-file approval"
  default_prompt: "Use $deliver-dual-audience-report for one reviewer to approve an initial plan with at least 4 naturally independent decisions, delivering precise Agent Markdown plus self-contained interactive Approval HTML; use a lighter workflow otherwise and never split content just to reach 4."
policy:
  allow_implicit_invocation: true
